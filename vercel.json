{
  "buildCommand": "turbo run build",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./packages/db/prisma/",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
} 